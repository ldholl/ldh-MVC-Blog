{{!-- <section class="homepage-display">
    <div class="post-list"></div>
    {{#each posts as |post|}}
    <div class="post-list"></div>
    {{> post-info post}}
    {{/each}}
</section> --}}

<section class="homepage-display">
    <div class="post-list columns is-multiline is-8">
        {{#each posts as |post|}}
        {{> post-info post}}
        {{/each}}
    </div>
</section>